'use client';
import { RouteLoader } from '@3de/ui';

export default function Loading() {
  return <RouteLoader showText loadingText="الدراسة رحلة طويلة، ولكن كل تعب فيها يقود إلى نور." size="lg" />;
} 